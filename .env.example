# Copy to .env and fill in values. Referenced by docker-compose.yml.

# Market data providers (optional; yfinance needs no key)
ALPHA_VANTAGE_API_KEY=
POLYGON_API_KEY=

# Pipeline tuning
RETRAIN_THRESHOLD=0.05
TICKERS=^GSPC,AAPL,MSFT
